import { Request } from "../@types/http";

export const routes = [
    {
        method: "POST",
        url: "/task",
        action: (req: Request, res: Response) => {
            
        }
    }
]